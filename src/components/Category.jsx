import React from 'react'
import { getData } from '../contex/DataContext'

function Category() {
    // const { data } = getData();
    return (
        <div>Category</div>
    )
}

export default Category